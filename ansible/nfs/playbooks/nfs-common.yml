---
- hosts: XXX
  become: yes

  vars_files:
    - vars/main.yml

  vars:
    list_of_packages:
      - nfs-common
      - libnfs-utils

  tasks:
    - name: Install common NFS packages
      ansible.builtin.XXX:
        name:
        state:

    - name: Create NFS directory
      ansible.builtin.XXX:
        path:
        state:
        owner:
        group:
        mode:
