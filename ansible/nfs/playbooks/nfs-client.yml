---
- hosts: XXX
  become: yes

  vars_files:
    - vars/main.yml

  tasks: 
    - name: Mount NFS share
      ansible.posix.XXX:
        fstype:
        path:
        src:
        opts:
        state:
